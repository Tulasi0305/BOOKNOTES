<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>

    <h1>Edit Book 📖</h1>

    <form action="/update/<%= book._id %>" method="POST">
        <label for="title">Book Title</label>
        <input type="text" id="title" name="title" value="<%= book.title %>" required>

        <label for="author">Author</label>
        <input type="text" id="author" name="author" value="<%= book.author %>" required>

        <label for="notes">Notes</label>
        <textarea id="notes" name="notes" required><%= book.notes %></textarea>

        <label for="rating">Rating (1-10)</label>
        <input type="number" id="rating" name="rating" value="<%= book.rating %>" min="1" max="10" required>

        <label for="dateRead">Date Read</label>
        <input type="date" id="dateRead" name="dateRead" value="<%= book.dateRead.toISOString().split('T')[0] %>" required>

        <button type="submit" class="btn-primary">Update Book</button>
    </form>

    <a href="/" class="btn-secondary">Back to Home</a>

</body>
</html>
